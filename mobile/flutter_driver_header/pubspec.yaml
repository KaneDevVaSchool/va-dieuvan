name: flutter_driver_header
description: Full-bleed driver dashboard header (sample, production-style)
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
